<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.badrul.user.bracexel">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Map"></activity>
        <activity android:name=".AdminDashboard" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".SignUp" />
        <activity android:name=".QuestionActivity" />
        <activity android:name=".PredictInspectionActivity" />
        <activity android:name=".PoEvaluationActivity" />
        <activity android:name=".BranchGrowthActivity" />
        <activity android:name=".NewUserDataActivity" />
        <activity android:name=".UserDataActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>